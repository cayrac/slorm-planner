import { DataReaper } from '../../model/data/data-reaper';

export const DATA_REAPER: { [key: number]: DataReaper } = {
    65: {
        constants: {
            base: [2],
            benediction: [],
            malediction: [],
        }
    }
}