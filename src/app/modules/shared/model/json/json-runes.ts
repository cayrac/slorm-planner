export interface JsonRunes {
    activationId: number | null;
    activationLevel: number;
    effectId: number | null;
    effectLevel: number;
    enhancementId: number | null;
    enhancementLevel: number;
}